<nav class="navbar navbar-expand-lg sticky-top custom-navbar">
    <div class="container">
        <a class="navbar-brand" href="/">
            <span class="brand-text">Ecom<span class="text-primary">Shop</span></span>
        </a>
        <button class="navbar-toggler custom-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navContent" aria-controls="navContent" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/products">Products</a>
                </li>
            </ul>
            <div class="d-flex align-items-center gap-3">
                <a href="/cart" class="cart-icon position-relative">
                    <i class="fa-solid fa-cart-shopping"></i>
                    <% if (cartCount> 0) { %>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            <%= cartCount %>
                        </span>
                        <% } %>
                </a>
                <div class="nav-auth d-flex gap-2">
                    <% if (currentUser) {%>
                        <a href="/orders" class="btn btn-outline-secondary btn-sm rounded-pill px-3 me-2">My
                            Orders</a>
                        <a href="/auth/logout" class="btn btn-outline-primary btn-sm rounded-pill px-3">Logout</a>
                        <% } else { %>
                            <a href="/auth/login" class="btn btn-outline-primary btn-sm rounded-pill px-3">Login</a>
                            <a href="/auth/register" class="btn btn-primary btn-sm rounded-pill px-3">Register</a>
                            <% } %>
                </div>
            </div>
        </div>
    </div>
</nav>